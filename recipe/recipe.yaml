schema_version: 1

context:
  version: "1.3.0"
  python_min: "3.11"
package:
  name: geolibre
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/g/geolibre/geolibre-${{ version }}.tar.gz
  sha256: 6b052671dbcafa326bdec26884b558f4851be2b23401c28369bbcfe87f337ac9

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling
    - pip
  run:
    - python >=${{ python_min }}
    - anywidget >=0.9
    - traitlets >=5
    - jupyter-ui-poll

tests:
  - python:
      imports:
        - geolibre
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  homepage: https://geolibre.app
  summary: 'GeoLibre in Jupyter: the full GeoLibre GIS app as an anywidget, with a leafmap-style Python API.'
  description: 'GeoLibre in Jupyter: the full GeoLibre GIS app as an anywidget, with a leafmap-style Python API.'
  license: MIT
  license_file: LICENSE
  documentation: https://geolibre.app
  repository: https://github.com/opengeos/GeoLibre

extra:
  recipe-maintainers:
    - giswqs
